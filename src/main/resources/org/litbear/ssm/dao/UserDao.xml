<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.litbear.ssm.dao.UserDao">
    <select id="getAllUsers" resultType="org.litbear.ssm.pojo.User" >
      SELECT * FROM user limit 20
    </select>
    
    <insert id="insertUser" parameterType="org.litbear.ssm.pojo.User">
        <selectKey keyProperty="id" resultType="Integer" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user (id, username, sex, birthday, address)
        VALUES (#{id}, #{username}, #{sex}, #{birthday}, #{address})
    </insert>

    <update id="updateUser" parameterType="org.litbear.ssm.pojo.User">
        UPDATE user SET
        username = #{username},
        sex = #{sex},
        birthday = #{birthday}
        address = #{address}
        WHERE id = #{id}
    </update>
</mapper>
